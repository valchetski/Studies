<UserControl x:Class="RWC2007.PoolControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox x:Name="PoolGroupBox">
        <GroupBox.Resources>
            <Style x:Key="DataGridStyle"  TargetType="DataGrid">
                <Setter Property="HorizontalAlignment" Value="Left"></Setter>
                <Setter Property="VerticalAlignment" Value="Top"></Setter>
                <Setter Property="IsReadOnly" Value="True"></Setter>
                <Setter Property="AutoGenerateColumns" Value="False"></Setter>
                <Setter Property="CanUserSortColumns" Value="False"></Setter>
                <Setter Property="Margin" Value="5"></Setter>
            </Style>
        </GroupBox.Resources>
        <DockPanel>
            <DataGrid x:Name="PoolDataGrid" Style="{StaticResource DataGridStyle}"
                      SelectionMode="Single" SelectionUnit="Cell" MouseDoubleClick="PoolDataGrid_MouseDoubleClick"/>

            <DataGrid x:Name="PoolResultsDataGrid" Style="{StaticResource DataGridStyle}"></DataGrid>
        </DockPanel>
    </GroupBox>
</UserControl>
